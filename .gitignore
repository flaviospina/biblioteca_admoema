# Dependências do frontend
# (a pasta frontend/dist/ com o build pronto É versionada de propósito,
#  para permitir publicar na HostGator sem precisar de terminal/npm)
frontend/node_modules/

# Configuração local com senhas (NUNCA versionar)
api/config.php

# Fotos enviadas pelos usuários (ficam só no servidor)
api/uploads/*
!api/uploads/.htaccess

# Diversos
.DS_Store
*.log
